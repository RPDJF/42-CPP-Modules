#ifndef TO_STRING_HPP
# define TO_STRING_HPP

# include <string>
# include <sstream>

// to_string:	converts template val into a string as return value
std::string	to_string(int val);
std::string	to_string(double val);

#endif